export async function processFeedback(feedback: string): Promise<void> {
    // Logika untuk memproses dan menyimpan umpan balik
    console.log('Feedback received:', feedback);
    // Di sini, Anda bisa menambahkan logika untuk menyimpan feedback ke database atau file
}
